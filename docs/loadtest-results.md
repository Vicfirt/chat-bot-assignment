# Load Test Results

> Regenerated by `python -m loadtest.run_load`. This committed run predates the
> `--warmup` flag and the two-model (`1b` + `3b`) pass; it is refreshed there.
> The bottleneck analysis does not depend on the exact figures.

- Requests: 100  |  Concurrency: 4  |  Errors: 0  |  Warmup discarded: 0
- Throughput: 7.57 req/s

## Latency (ms)

| p50 | p90 | p95 | p99 | mean | max |
|-----|-----|-----|-----|------|-----|
| 19.2 | 656.3 | 1066.1 | 10450.2 | 528.4 | 10561.6 |

## Per-node mean (ms)

- retrieve: 694.7
- triage: 0.0
- calculate: 0.0
- synthesize: 0.0
- guardrails: 0.0
- validate: 0.0
- plan: 0.0

## Bottleneck

This run (`LLM_MODE=dummy`): `retrieve` is the dominant node (mean 694.7 ms) — dense + BM25 + cross-encoder rerank over the embedded Chroma index, lock-serialised. Every other node is ~0 ms because the LLM is stubbed. This isolates the retrieval path.

Deployed system (`LLM_MODE=ollama`): the bottleneck is **`synthesize` — LLM generation — by two orders of magnitude.** Each request makes three `llama3.2:3b` CPU calls: `triage` (~10-30 s), `expand_query` (~10-30 s), `synthesize` (~3-4 min). Retrieval and the rest are < 1 s combined.

## Optimization recommendations

1. **Collapse three LLM calls to one.** `triage` and `expand_query` each cost a full `llama3.2:3b` round-trip. `triage` can be embeddings/rules-only (the deterministic guard already overrides the model for the common cases); `expand_query`'s LLM rewrite buys little over the domain-hint map. Rules-only for both removes ~20-60 s/request in `ollama` mode. Highest impact on the real system.
2. **End-to-end response cache** keyed on normalised question + route, checked before the graph runs. The current caches (`app/rag/cache.py`) stop at the RAG subgraph, so `synthesize` re-runs on every repeat; a full-response cache returns a repeat query in ~1 ms instead of ~4 min.

## Note on the tail

- Retrieval is serialised by one process-wide lock (see README "concurrency"), so at concurrency > 1 the first requests queue behind the one-time model + HNSW warmup — that is the p99/max tail here. Steady state (p50) is unaffected.
